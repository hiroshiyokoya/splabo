# Changelog

## [Unreleased]

### Changed

- ダウンロードページの URL を [splaboon.pages.dev](https://splaboon.pages.dev/) にしました
- 紹介ページの説明を、いまの機能（ギアコーデ・パネル保存・非公式であることの明示）に合わせました
- 紹介ページから、まだ案内しない splabo-viewer の記述を外しました
- 紹介ページの AI 分析を、右下の下段パネルに分けて書くようにしました
- 紹介ページの用語をナビに合わせ、図鑑・全世界・ホバーなどの余分な説明を外しました
- README と llms.txt の「ブキ図鑑」「ステージ図鑑」も、ナビと同じ「ブキ」「ステージ」に揃えました
- 紹介ページの『できること』を、左上バトル・ダッシュボード／右上ブキ・ステージ・ギアにし、右下の自動取得と AI 分析を一つの枠にまとめました
- 紹介ページの右上と右下を入れ替え、分量のバランスを整えました

## [0.10.4] — 2026-08-14

### Changed

- 散布図のツールチップに、ブキの**サブとスペシャル**のアイコンを出すようにしました（ダッシュボード・環境分析。保存 HTML も同じ）
- ヒートマップの**ブキ軸ラベル**をホバーすると、名前とサブ／スペシャルのアイコンが出るようにしました（ダッシュボード・環境分析）
- 自動取得に失敗したときの通知に**時刻**を出すようにしました（何度も溜まったとき、いつ失敗したか分かるように）
- 線グラフの **回/バトル** など、勝率以外の軸の目盛を整数にしました

### Fixed

- ステージ図鑑で **平均塗り** で並び替えできるようにしました（一覧ヘッダ・パネルの並び順）

## [0.10.3] — 2026-08-13

### Added

- 散布図の点を**ブキ画像**にできるようにしました。ダッシュボードと環境分析で、ブキ別の散布図のときに選べます。大きさは小・中・大から選べます
  - 画像が重なっているところは、**カーソルを近づけるとばらけて** 1 つずつ見えます。カーソルを外すと元に戻ります
  - **保存した HTML でも同じように動きます**

### Changed

- 散布図の**ツールチップにブキ画像**が出るようになりました
- ツールチップを**半透明**にし、他の点にできるだけ重ならない位置へ出すようにしました
- 散布図の**軸の範囲をデータに合わせて狭く**しました。点がグラフ全体に広がって読みやすくなります
- グラフの設定で、**選んだ内容によって使えなくなる項目を出さない**ようにしました（灰色で残していました）

### Fixed

- グラフをクリックすると**黒い枠**が出ていたのを直しました。画像として保存すると枠まで写っていました

## [0.10.2] — 2026-08-12

### Changed

- 自動取得で**バトルが取れなかったときは通知しなくなりました**。取れたときだけお知らせします
- Windows のインストーラで、**「デスクトップにショートカットを作成」の初期チェックを外しました**（項目は残っています）

## [0.10.1] — 2026-08-10

### Fixed

**データ取得**

- **取得の間隔は、長くするほど失敗しやすくなっていました。** 既定を 24 時間ごとから 1 時間ごとに変えました
  - 認証に使うトークンは約 2 時間で切れます。24 時間おきだと**取りに行くたびに必ず認証をやり直す**ことになり、そのぶん失敗する機会が増えていました。1 時間おきならトークンが生きているあいだは認証なしで済みます
  - **以前から使っている場合、保存された設定がそのまま残ります。** 設定画面の「自動取得の間隔」を確認してください
- 取得元が一時的に応答しないとき、**5 秒後に 1 回だけやり直す**ようにしました。認証の回数に余裕があるときだけ試します
- 認証の待ち時間が短すぎて、**通りかけていた認証を自分から打ち切って**いました。認証だけ長めに待つようにしました
- 認証の回数制限（1 時間あたりの上限）まわりの案内を直しました
  - **まだ回数が残っているのに「◯時以降に再試行できます」と出る**ことがありました
  - 回数は種類ごとに別々に数えられているのに別の種類の残りを見ていたため、**案内が出ない**ことがありました
  - 回数が残っていないときは、**通信する前に止まる**ようにしました。試すだけ回数を減らしていました
  - 回数制限に達したときは**エラー通知を出さず**、ログに残すだけにしました。故障ではなく、待つ以外にできることが無いためです
- 取得に失敗したときの**理由がログに残る**ようになりました。これまでは取得処理からの説明を捨てていたため、「失敗しました」以上のことが分かりませんでした

**AI 分析**

- シーズンごとに集計したとき、**シーズンが新しい順に並ばない**ことがありました。シーズン名は名前順に並べても時系列にならないためです

## [0.10.0] — 2026-08-08

### Changed

**AI 分析**

- **仕組みを作り直しました。** 聞きたいことを日本語で入力すると、AI が **SQL を書き、集計はこの PC の中だけで実行**します。結果は表かグラフで出ます
  - **数値をアプリが集計するようになりました。** これまでは AI が計算した数値をそのまま出していたため、**AI が間違えた数字が表示される**ことがありました
  - **バトルの内容は AI に送りません。** 送るのは質問文・データの構造の説明・件数と期間の範囲です。表やグラフの形を決めるときだけ、集計結果の先頭 20 行を送ります
  - 相関のような分析もできます（例:「勝率と最も相関の高いバトル指標は？」）
  - 「行はパワー帯、列は順位」のように**見せ方を指定**できます
  - 実行できるのは読み取りだけで、時間のかかりすぎる集計は自動で打ち切ります

**絞り込み**

- 期間の絞り込みで、**シーズンを名前で選べる**ようにしました（新しい順）。これまでは「今シーズン」しか選べず、過去のシーズンを見るには日付を手で入れる必要がありました。バトル・ブキ図鑑・ステージ図鑑と環境分析のどちらでも使えます
- 環境分析の期間を、バトル・ブキ図鑑・ステージ図鑑と同じ**ボタン並び**に揃えました
- **ブキ選択のカテゴリと個々のブキの入れ子**が見た目で分かるようにしました（カテゴリ見出しに背景、ブキは一段下げて罫線）

**表記**

- 画面・ドキュメントの **「武器」を「ブキ」に統一**しました。Splatoon の表記に合わせています（タブ名・絞り込み・保存画像のキャプションなど）

**環境分析**

- グラフ設定に **「表示するブキ」** を追加しました。**集計はそのままで、グラフに出すブキだけを選べます**
  - 上の絞り込みにあるブキとは意味が違います。あちらは**そのブキがいるバトルだけを集計**するので、ピック率や勝率の母数も変わります
  - 散布図（ブキ別）とヒートマップ（行か列がブキ）で使えます

### Fixed

- 環境分析のヒートマップを**画像として保存**したときの表示を直しました
  - 列が少ないとき（スペシャル × ルールなど）に**右側へ大きな余白**が残っていました
  - **スクロールバーが写り込み、表が途中で切れて**いました。全体が写るようになります
  - 画像の**下に余白**が空いていました
  - 絞り込み条件の文字が**右上のロゴに重なる**ことがありました
- データ取得が **`Too many attempts to authenticate`** で失敗しやすい問題を直しました。取得に失敗したとき認証をやり直しており、**1 時間あたりの認証回数の上限**（nxapi 側の制限）を使い切っていました。失敗時には、次に試せる時刻を表示します
- 環境分析の**ブキの選択肢が出るまで 50 秒ほどかかっていた**のを、数秒に短縮しました（集計の書き方を変え、索引を追加）。長いあいだ「選択肢がありません」と表示されるため、**無いように見えて**いました
  - 選択肢を取りに行っている間は「読み込み中...」と出るようにしました
  - 初回起動時に索引を作るため、環境データを取り込み済みの場合は起動が一度だけ遅くなります

## [0.9.10] — 2026-07-30

### Changed

**画面レイアウト**

- **バトル・武器図鑑・ステージ図鑑**が、ウィンドウを広げたぶんだけ横に広がるようになりました（これまでは途中で頭打ちになり右側が空いていました）。一覧表示・パネル表示・統計パネルのすべてに効きます
- 逆にウィンドウを狭めたときは、**統計パネルが 6 列 → 3 列 → 2 列** と折り返すようになりました（これまでは 6 列のまま痩せ続けて数字が潰れていました）

**統計パネル**

- **キルレのカッコ内に貢献キルレ**を出すようにしました（`1.25 (1.53)`）。あわせて、カッコの中身が分かるようにラベルを **「平均キル (アシスト)」「キルレ (貢献)」** に変えました

**散布図**

- **目盛りを切りのいい数字**にしました。勝率は 5% 刻みなど、軸の範囲に応じて 1・2・5 系の刻みで並びます（これまではログ軸だけ対応していました）
- **勝率 50% / キルレ 1 の基準線**（破線）を引くようにしました。ダッシュボードのカスタム散布図にも出ます
- 軸の範囲は**データに合わせて自動**で決め、そのうえで**基準値（勝率 50% / キルレ 1）の目盛りは必ず出す**ようにしました
- ツールチップで、**バトル数に勝敗の内訳**を添えるようにしました（`バトル数: 123 (70 勝 50 敗 3 分)`）。ヒートマップと同じ書き方です

**パネルの保存**

- キャプションを **ロビー → 期間 → ルール・武器などの絞り込み → 該当バトル数** の順に統一しました。**ダッシュボードにも該当バトル数が入ります**（これまで環境分析にしかありませんでした）。条件が長いときは打ち切らず折り返します
- ロビーやルールを**複数選んだときの値の区切りを `, `** にしました。項目の区切り（` / `）と見分けが付きます
- **同じ場所に同名のファイルがあるときは、既定のファイル名を `-2` `-3` … と連番**にするようにしました（保存先が分かっているとき。アプリ起動後の 1 回目は従来どおりです）

### Fixed
- パネルの保存画像で、ヒートマップなど**列数で幅が決まるグラフ**のときに右側へ大きな余白が残る問題を修正しました。列が多くて画像からはみ出していたケースや、斜めの列ラベルが右端で切れるケースも収まります

## [0.9.9] — 2026-07-27

### Fixed
- 散布図のサイズ凡例で、貢献キルレなど最小値が 0 付近のとき **0.00 が重複表示**される問題を修正しました

### Changed
- 環境分析ヒートマップで、**武器を絞り込んだとき**はステージ×ルールなど非武器同士の軸でも勝率・キル系を選べるようにしました（ピック率は従来どおり武器系の軸が必要です）
- 環境分析ヒートマップのセル指標から **KO率** を外しました
- 武器カテゴリの **リールガン** を公式どおり **シューター** に統合しました（L3/H3）
- 環境分析の武器絞り込みを、バトル画面と同様に **カテゴリ見出しで一括選択**でき、並びもカテゴリ → 件数順にしました
- 環境分析でフィルタをかけたとき、条件に合う **該当バトル数** を表示するようにしました（保存画像のキャプションにも反映）
- 散布図のカテゴリ色分けを**カラーテーマに追従**させました（デフォルトは従来どおり、Solarized ではアクセント色）
- 散布図のツールチップ対象ドットから **青い外側リング** を外し、黒枠だけにしました

## [0.9.8] — 2026-07-27

### Added
- ダッシュボード・環境分析の各パネルを **PNG / HTML** として保存できるようにしました。保存ボタンはパネル右上。タイトル・絞り込み条件・splabo ロゴ・クレジット（版と日付）が入り、PNG では角丸の外側は透明です。HTML は単体ファイルでオフライン表示でき、散布図はブラウザ上でホバーするとツールチップが出ます
- 散布図はツールチップを出したまま保存できます。対象ドットは枠で強調し、保存時は他ドットと被りにくい位置へ寄せて半透明にします

### Changed
- 環境分析の集計から**投稿者本人を除外**し、残り 7 人を母数にしました（stat.ink の全体統計と同じ数え方）。勝率・ピック率は再取得なしで新しい集計になります
- キル・デス・アシスト・塗りポイントを**投稿者を除く 7 人分すべて取り込む**ようにしました。過去に取り込んだデータは 1 人分しか記録が無いため、「全期間再取得」で母数が増えます
- 「A1・B1」といった内部用語の注記をやめ、「投稿者を除く 7 人分」という表記に統一しました
- 保存画像・HTML に焼き込む**期間**を、相対表記（今シーズン / 30日 など）ではなく **YYYY-MM-DD~YYYY-MM-DD** の絶対日付にしました。UI のフィルタ表示は従来どおりです
- 保存キャプションやチャートタイトルなどの**記号を半角に統一**しました（` / `・`~`・`()` など）
- 環境分析でフィルタ変更時、**グラフ更新中**をチャート上でも表示し、連続変更で古い集計結果が後から差し替わらないようにしました
- 散布図の**サイズ凡例**を、データの生の最小・中間・最大ではなく **1・2・5 系の切りのいい数字**で出すようにしました
- ダッシュボードの**武器別棒グラフ**（固定＋カスタム）の並び替えを、先に上位を切る方式から**選んだ指標で全件ソートしてから上位表示**に変更。勝率順は 5 戦以上のみ。カスタムは同じキー再クリックで昇順/降順を切り替えられます

## [0.9.6] — 2026-07-26

### Added
- 環境分析に**武器・ステージの絞り込み**（複数選択）
- 環境分析ヒートマップで**列見出しクリックによる行の並べ替え**（「既定の並び」で解除）。行/列に**武器カテゴリ・サブ・スペシャル**も選べる
- 散布図で**武器カテゴリ・サブ・スペシャルを色×形で区別**（環境分析・ダッシュボード）。ダッシュボードではドット単位にも選べる
- 環境分析の散布図に**軸ごとのログスケール切替**

### Changed
- ダッシュボード等の**期間フィルタの既定を「今シーズン」に**（従来は全期間）
- フィルタ表記を**モード → ロビー**に統一。環境分析のフィルタ並びも期間→ロビー→ルール→武器→ステージに揃えた
- 複数選択プルダウンを**外側クリックで閉じる**ように
- 環境分析の**ピック率を小数2桁表示**に。ステージ散布図は**KO率を外し、勝率・KDA系を追加**（武器絞り込み時）
- 武器属性の静的マスターを同梱し、カテゴリ集計で**「(未分類)」「(不明)」が出ない**ように
- 散布図のツールチップを、**他のドットと重なりにくく外側へ逃げる位置**へ自動配置

## [0.9.5] — 2026-07-25

### Added
- 折れ線グラフの**複数系列・左右2軸**対応（軸グループ名と凡例の左右寄せ付き）
- メトリクスに**貢献キル**・**貢献キルレ**を追加（カスタムグラフ・環境分析）
- 期間プリセットに**1年・180日**を追加し、文言を短く統一（`全期間 / 今シーズン / 1年 / 180日 / 30日 / 7日 / カスタム`）
- 環境分析の共通フィルタに**クリア**ボタン
- 散布図にサイズ・色の**凡例**
- 設定を**連携 / データ / 表示 / AI**の4タブに再編
- 起動時・自動取得で**ギアも更新**
- バトル・武器・ステージで、絞り込みと表示切替をスクロール中も**常時表示**（ダッシュボードの統計カードも同様）

### Changed
- 武器・ステージ図鑑の見た目と情報量を揃え、**平均アシスト**・勝敗表示を追加（K → A → D）
- バトル数カレンダーは期間外を描かず、今日はバトル0でも常に表示
- ダッシュボードのカスタムグラフを、グラフごとの適幅で動的配置
- 環境分析を改善（設定の永続化、軸ラベルの色、散布図のアイコンなど）
- 画面表記を統一（キルレ、最新データを取得、期間ラベルなど）

### Fixed
- 外部サービスが一時不調なだけのときに誤って再ログインを促す問題
- 外部サービス無応答時に取得処理がハングする問題

## [0.9.4] — 2026-07-22

### Added
- **散布図の軸をログスケールに切り替えられる**ようにしました。バトル数や勝数のように少数派が原点付近に潰れてしまう軸や、キルレのように「0.5 倍」と「2 倍」を対称に見たい軸が読みやすくなります。グラフごとに X 軸・Y 軸それぞれ設定できます
  - ログ軸には 0 以下・無限大の点を載せられないため、該当する点は除外し、何件除いたかをグラフの下に表示します
  - 勝率のような比率のメトリクスではログにする意味がないため、設定側で選べないようにしています

### Changed
- **ヒートマップの「データなし」「サンプル不足」の斜線を控えめ**にしました。これまでは実際のデータより濃く、チャートの中で最も目立つ要素になっていました。どちらも中立色と背景色だけで描くようにして、実データの邪魔をしないようにしています
- バトル数カレンダーで、**月が月曜から始まるときも空の列を挟む**ようにしました。これまでこの場合だけ月の区切りが普通の週の変わり目と見分けられませんでした

### Fixed
- カスタムグラフのツールチップで**同じ項目が 2 行重複して表示される**不具合を修正しました（散布図でサイズと色に同じ項目を選んだとき、およびヒートマップでメトリクスに「バトル数」を選んだとき）

## [0.9.3] — 2026-07-16

### Changed
- **ヒートマップの配色を刷新**しました。「薄い＝少ない・濃い＝多い」で読めるようになり、どのテーマでも濃淡の向きが揃います
  - **勝率はピンク ↔ 青の 11 段階**。ダッシュボードの勝率グラフと同じ色に揃えました。真ん中（引き分け付近）は無彩色、勝ち越し・負け越しが極端なほど濃くなります
  - **勝数・平均キルなどはメトリクスの意味ごとに色分け**しました（多いほど良い = 緑 / 多いほど悪い = 赤 / 良し悪しの無い量 = オレンジ）。濃さで 7 段階
  - カレンダーとカスタムグラフの散布図も同じ配色に揃えました
- **データが無いセル・サンプルが足りないセルを斜線で表示**するようにしました。これまでは実際のデータより目立つグレーで塗られていました
- 勝率のカラーバーから中央の「50%」ラベルを削除しました（バーの右に並ぶだけで中央を指せていませんでした）
- ヒートマップの勝数・平均系で、カラーバーの範囲を整数で表示するようにしました（「3.2 – 7.8」→「3 – 8」）

### Fixed
- 散布図で**サイズを「（一定サイズ）」にして保存したグラフを編集で開くと「バトル数」に変わってしまう**不具合を修正しました。そのまま保存するとサイズ指定が付いてしまっていました
- ヒートマップで**合計系のメトリクス（「キル数（合計）」など）を選ぶと、すべてのセルが空になる**問題に対応しました。2 軸のクロス集計では合計を出せないため、ヒートマップの選択肢から外しています（カレンダー・折れ線では引き続き選べます）

## [0.9.2] — 2026-07-16

### Fixed
- **環境分析のキル・デス・アシスト・塗りポイントの集計が誤っていた不具合を修正**しました。stat.ink データの取り込み時に列の位置がずれており、特に「平均デス」が実際にはデス以外の値になっていました。正しい値にするには、環境分析の「全期間再取得」から取り込み直してください
- アプリ起動直後、設定タブを一度も開いていないと、常駐（トレイ）・自動取得・stat.ink 設定が有効にならないことがある不具合を修正しました
- **ギアコーデがアプリのテーマに追従しない不具合を修正**しました。Solarized Light などを選んでも、ギアコーデタブだけ配色が暗いままでした

### Added
- **環境分析のヒートマップにセル指標を追加**しました（平均キル・平均デス・キルレ・平均アシスト・貢献キルレ・平均塗りポイント）
- 環境分析に「**全期間再取得**」ボタンを追加しました（取得済みデータを削除して stat.ink から取り込み直します）

### Changed
- ヒートマップ（環境分析・ダッシュボード）で、**ルールの並び順をガチ系を先・ナワバリを最後**に統一しました
- ダッシュボードのヒートマップのツールチップに、**バトル数の勝敗内訳**（◯勝 ◯敗）を表示するようにしました
- 一覧テーブルのスクロールバーが常にテーブル内に表示されるようにしました
- **武器図鑑・ステージ図鑑のパネルをホバーしたときの見た目をギアコーデと統一**しました。カードの背景が一段変わって浮き上がります
- ギアコーデのサブタイトル表記を「Geartoon」に変更しました

## [0.9.1] — 2026-07-14

### Fixed
- **KO 負け（および被 KO 率）が実際より多く表示される不具合を修正**しました。時間切れで決着したバトルが KO 負けに数えられていました。取り込み済みのデータもアプリ起動時に自動で補正されます

### Changed
- 武器図鑑カードの勝敗表示を整理しました。KO 勝ち・KO 負けは勝ち・負けの内訳として「勝ち N (M)」の形（カッコ内が KO の数）で表示します

## [0.9.0] — 2026-07-14

### Changed
- **画面構成を刷新しました。** サイドバーの「ダッシュボード」と「バトルログ」を **「戦績」タブ**にまとめ、タブの中で「ダッシュボード / 一覧」を切り替えられるようになりました
- **武器図鑑・ステージ図鑑に「パネル / 一覧」の切り替え**を追加しました。一覧では列見出しをクリックして並び替えができ、絞り込みも使えます
- **武器図鑑・ステージ図鑑が上部の絞り込み（期間・モード・ルール・結果）に対応**しました。期間を変えるとバトル数・勝率・平均キル / デスがその期間の値に切り替わります
  - ※ 熟練度・通算勝利数・総塗ポイントは任天堂から取得する累計値のため、絞り込みを変えても全期間の値のままです（「全期間」バッジで区別しています）
- メニューの名前と並びを整理しました（戦績 / 武器図鑑 / ステージ図鑑 / ギアコーデ / 環境分析 / AI分析 / 設定）
- 選んだ表示（ダッシュボード / 一覧、パネル / 一覧）はアプリを閉じても保持されます
- バトル数カレンダーで、**月の 1 日から新しい列が始まる**ようにしました。月の区切りが一目で分かります
- 環境分析の期間に「**今シーズン**」を追加しました
- stat.ink へアップロードする際のクライアント名を `splabo/chartoon` に更新しました

### Added
- 武器の詳細に「**勝率の良いステージ**」を追加しました（5 戦以上プレイしたステージが対象）

### Fixed
- **フェスマッチ（チャレンジ）がフェスマッチ（オープン）として記録される不具合を修正**しました。取り込み済みのデータもアプリ起動時に自動で振り直されます
- トリカラマッチをバトル一覧に表示しないようにしました（他とルールが大きく異なるため）

## [0.8.1] — 2026-07-07

### Added
- アプリ情報（About）に作者（GitHub アカウント）とフィードバックフォームへのリンクを追加

### Changed
- データ取得の導線を整理。サイドバーの取得ボタンを「最新データを取得」に変更し、戦績とギアをまとめて取得するように統一しました（ギアタブにあった重複する取得ボタンは撤去）
- ロゴを刷新しました（SpLabo）

### Fixed
- 認証（f-token 取得）が外部サービスの一時的な障害で失敗したとき、自動でリトライして回復するように改善しました
- 旧バージョンから更新した際、移行完了後に旧アプリのアンインストールを案内するようにしました（旧バージョンと同時に起動してしまうのを防ぎます）

## [0.8.0] — 2026-07-07

### Changed
- **戦績とギアを単一の splabo アプリに統合**しました。これまで内部的に分かれていたデータ保存先を 1 つにまとめ、旧 chartoon / geartoon のデータ（戦績・ギア・設定）は splabo 初回起動時に**自動で移行**されます。元データはそのまま残るため安全で、再ログインも不要です。移行の結果は初回起動時に通知されます。

> 📌 **Windows で旧バージョン（v0.7 以前）から更新する方へ**: exe インストーラで「NSIS Error / Error launching installer」が出る場合は、`.msi` を使うか、旧「splabo」を先にアンインストールしてから exe を実行してください（移行初回のみ・データは保持されます）。

## [0.7.0] — 2026-07-07

### Added
- **ギアタブを新設**。所持ギアの取得・一覧表示と、コーデ（ギア構成）組みができます（旧 geartoon のギア管理機能を統合）

### Changed
- アプリ名を **splabo** に変更しました。ロゴを刷新（chartoon と geartoon を束ねた新ブランド）
- 設定の保存方法を改善。将来の統合版に向けて設定値を冗長化して保持し、更新時のキー喪失を防止します

> 📌 **既存ユーザーへ**: この更新から chartoon はギア管理アプリ geartoon と統合し **splabo** になりました。設定・戦績データの保存先はこのバージョンでは変わりません（そのまま引き継がれます）。将来の統合版で保存先が変わる際は自動で移行されます。

## [0.6.7] — 2026-07-03

### Added
- ステージ図鑑：ステージをクリックすると詳細モーダルが開き、ルール別統計（バトル数・勝率・平均K/D/A・KO率）と、そのステージでの勝利数トップ・勝率トップの武器を表示
- ステージ図鑑：平均K・平均D カラムを追加
- 自動取得の間隔プルダウンに「4時間ごと」を追加

### Changed
- ダッシュボードのバトル数カレンダーを GitHub 貢献グラフ風にリファイン。月ラベルを追加し、色はエメラルドグリーンで低カウントから高カウントへのグラデーションに
- 武器詳細モーダルの取得不可項目を chartoon が把握したローカル集計に置き換え。勝・KO勝・負け・KO負け・平均塗り・総塗りをローカルバトル履歴から表示（熟練度のみ Nintendo 依存で `—` のまま）

### Fixed
- ダッシュボードのカスタムグラフのうちヒートマップ・散布図が、アプリを再起動すると描画されなくなる問題を修正（localStorage 復元時に一部フィールドが欠落していた）
- stat.ink にアップロードしたバトルがクライアント一覧で `chartoon 0.6.6` のように `v` なしで表示される問題を修正

## [0.6.6] — 2026-06-22

### Fixed
- Nintendo Switch Online アプリのバージョン要件が 3.4.0 に引き上げられたことに伴いバトルデータ取得が `[znc] Upgrade required` で失敗していた問題を修正

### Changed
- 起動時の自動取得・定期取得（スケジューラ）でも、サイドバーの「バトルデータを取得」ボタンが「取得中…」表示・無効化されるよう統一
- stat.ink へ送信する User-Agent のバージョン部分を `v` 始まりに変更（`chartoon/0.6.6` → `chartoon/v0.6.6`）。他クライアントの表示形式に統一

## [0.6.5] — 2026-06-03

### Fixed
- **【重大】** stat.ink から一括インポートしたバトルが自動アップロードで再送され、重複・無効な送信が発生する問題を修正。インポート機能を使った場合に大量の重複アップロードが発生していました。該当する方は最新版への更新をおすすめします
  - 既にインポート済みのバトルも、起動時のマイグレーションで自動的に補正対象となります（再アップロードされません）

## [0.6.0] — 2026-06-03 【取り下げ】

> ⚠️ 一括インポートしたバトルが stat.ink へ自動で再送される重大な不具合（v0.6.5 で修正）を含むため、このリリースは取り下げました。v0.6.5 以降をご利用ください。

### Added
- **環境分析タブを新設**。[stat.ink](https://stat.ink/) の公開バトルデータ（全世界のプレイヤー投稿）を取り込み、コミュニティ全体の環境を分析できます
  - 武器/ステージ別の散布図。X/Y 軸の指標（ピック率・勝率・平均キル/デス/アシスト・キルレ・塗りポイント等）を自由に選択でき、軸は自動スケール
  - マトリクスヒートマップ（武器×ステージ、ルール×ステージ 等。セルは勝率・ピック率・KO率・バトル数）
  - ロビー・ルール・ゲームバージョン・ウデマエ帯・Xパワー帯・期間（全期間/直近1年/180日/30日/カスタム）で絞り込み
- **フィルタの複数選択に対応**。モード・ルールを複数まとめて OR 絞り込みできます（例: ガチエリア＋ガチヤグラ、バンカラ＋Xマッチ）

### Changed
- モード/ルールのフィルタをチェックリスト型に変更。バンカラ/フェスのオープン・チャレンジを個別に選べるようになりました
- 勝率 55% 以上の棒グラフの色を青系に変更（勝数の緑と区別しやすく）
- グラフの軸目盛り・ラベルを明るく太字にして見やすく統一
- ダッシュボードのカレンダーを初期表示で最新日（右端）から表示し、マス目を縮小

### Fixed
- イベント（リーグ/チャレンジ）マッチの stat.ink アップロードが失敗していた問題を修正
- About モーダルの幅を広げてテキストの折り返しを改善

## [0.5.3] — 2026-05-29 【取り下げ】

> ⚠️ 一括インポート機能（本バージョンで追加）に、取り込んだバトルが stat.ink へ自動で再送される重大な不具合（v0.6.5 で修正）があったため、このリリースは取り下げました。v0.6.5 以降をご利用ください。

### Added
- フェスマッチ（オープン/チャレンジ）を集計に対応。既存バトルも自動マイグレーションで遡及
- イベントマッチ（eventBattleHistories）の取得に対応
- stat.ink から過去バトル履歴を一括インポート（設定タブから実行可能）
- カレンダーヒートマップのメトリクスに日次合計（キル数・デス数・アシスト数・塗りポイント）を追加
- ダッシュボードのステージ別棒グラフをプレイ済み全件表示に修正
- 設定タブに「武器データを更新」ボタンを追加
- カレンダーのツールチップをバトル数・勝数・負数・勝率の 4 行に変更

### Changed
- 「キル」「デス」「アシスト」の表記を「キル数」「デス数」「アシスト数」に統一（平均系は据え置き）
- モードフィルタに「フェス」「イベント」を追加

## [0.5.0] — 2026-05-27

### Added
- ステージ図鑑タブを新設
- 武器詳細モーダルをバトル統計パネルに刷新、ステージ名をフル表示
- 武器図鑑のソート項目に「勝数」「平均キル」を追加、詳細モーダルの順序を調整

### Changed
- 散布図でサイズ一定のとき、武器/ステージ単位は大きめのドットに（バトル単位は変更なし）

### Fixed
- `db_list_weapons` が旧テーブルを参照していた問題を修正、開発ビルドのバージョン表示・ハードコード版数を整理

### Known Issues
- nxapi 同梱の `WeaponRecordQuery` 持続クエリハッシュが SplatNet アプリ v5 移行で廃止。武器熟練度・通算勝利数・総塗りポイントが現状取得不可。nxapi の更新待ち

## [0.4.1] — 2026-05-27

### Added
- フィードバックフォーム（Google Form）へのリンクを 3 ヶ所に追加: README / 紹介ページ (chartoon.pages.dev) / リリースノートフッター

## [0.4.0] — 2026-05-27

chartoon 初回リリース。

Splatoon 3 のバトル履歴を取得・可視化する OSS の PC アプリ。nxapi 経由で SplatNet 3 からバトルデータを取得し、自由にカスタマイズできるチャートでプレイを分析できます。

🌐 ダウンロード: https://chartoon.pages.dev/

### ダッシュボード・カスタムグラフ
- 主要指標（バトル数・勝率・キル/デス・キルレシオ）と、武器・ステージ・ルール・モード別の集計
- 期間フィルター、フィルターバーはバトルログと共通
- カスタムグラフ: 線・棒・散布図・ヒートマップ・カレンダー
- 集計軸（武器・ステージ・モード・ルール・サブ/SP・期間・味方武器・相手武器）と表示メトリクスを自由に組み合わせ
- ドラッグ＆ドロップで並び替え

### バトルログ・武器図鑑
- 履歴一覧（検索・フィルター・ソート）
- 詳細モーダルでチーム編成・ギア・スコアボード・ランク / X パワー変動を確認
- 所持武器の勝率・サブ/SP 一覧

### データ取得・自動化
- Nintendo アカウント認証（nxapi）で取得
- バトル履歴の全件ページネーション取得
- 15 分〜24 時間ごとの自動取得スケジューラー、トレイ常駐、完了通知

### stat.ink 連携
- 取得バトルを自動アップロード（s3s 互換ペイロード、UUID v5 で重複排除）
- ギアパワー・ウデマエ前後・X パワー変動も送信

### AI 分析
- 自然言語からチャート生成
- OpenAI / Gemini / Claude / Grok 対応、価格情報付きモデルプリセット

### その他
- アプリアイコン・ロゴデザイン
- README + Cloudflare Pages 紹介ページ（SEO / OGP / llms.txt 対応）
- リリース自動化 CI、PR 検証 CI
