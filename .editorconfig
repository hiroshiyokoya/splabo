# 改行はリポジトリの .gitattributes と同じく LF。
# Windows のエディタが CRLF で上書きして git status が汚れるのを防ぐ。
root = true

[*]
end_of_line = lf
charset = utf-8
insert_final_newline = true

[*.{bat,cmd,ps1}]
end_of_line = crlf
